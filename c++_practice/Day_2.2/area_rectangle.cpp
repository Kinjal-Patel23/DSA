#include <iostream>

using namespace std;

int main()
{
    float w, l, area;

    cout << "Enter the value of length: ";
    cin >> l;

    cout << "Enter the value of width: ";
    cin >> w;

    area = (l * w);

    cout << "Area of the rectangle of:" << area;

}